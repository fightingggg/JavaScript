Math.ceil(): 이 함수는 주어진 숫자를 '올림'하여 가장 가까운 정수를 반환합니다. 예를 들어, Math.ceil(3.4)는 4를 반환하고, Math.ceil(5.9)는 6을 반환합니다.

Math.floor(): 이 함수는 주어진 숫자를 '내림'하여 가장 가까운 정수를 반환합니다. 예를 들어, Math.floor(3.4)는 3을 반환하고, Math.floor(5.9)는 5를 반환합니다.

Math.round(): 이 함수는 주어진 숫자를 '반올림'하여 가장 가까운 정수를 반환합니다. 예를 들어, Math.round(3.4)는 3을 반환하고, Math.round(3.5)는 4를 반환합니다.

------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
